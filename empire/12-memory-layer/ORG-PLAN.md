# ORG PLAN — 12 Recall Foundry (org: devtools infrastructure)
Mission: the memory layer that runs where your agent runs.

## Placement
Shelf: AI memory infra, UNDER the funded clouds on price and friction (Mem0/Supermemory/Letta = cloud + meters), ABOVE DIY JSON files (no recall, no evals). The pitch in one line: "Supermemory's ideas, localhost's bills." Never fight them on model quality — fight on deployment: offline, readable, fixed-cost.

## Pricing architecture
- Local core $0 forever (OSS, MIT — stars are the funnel).
- Sync $19/mo (hero: backup + multi-machine + share links).
- Team $99/mo (5 seats, shared tenant memory, audit log).
- Enterprise: redaction proofs + retention exports + on-prem review (call us).
Math: 100 sync × $19 = $1.9k MRR funds everything; infra cost ~$0 (user machines hold data).

## Packaging
One-file SQLite ("your brain, as a file"), Lab UI demo in-app, golden-eval badge (precision@3 published like uptime), MCP one-click installs.

## Channels (ordered)
1. OSS launch (HN Show + Reddit r/LocalLLaMA — local-first is religion there). 2. MCP directory + Cursor/Claude docs. 3. Indie agent-builder outreach (our own ICP mirror). 4. YC application with traction (stars + sync MRR).

## 30-60-90 + kill
D30: MVP + goldens + 100 stars. D60: MCP shape + 5 pre-sales. D90: $500 sync MRR or pivot wedge (eval-tooling spinoff per thesis T4). Kill rule: precision@3 <0.7 after tuning = retrieval approach wrong, add sqlite-vec.
Needs: nothing (zero keys). Later: domain + $5 VPS for sync relay (owner decision).
