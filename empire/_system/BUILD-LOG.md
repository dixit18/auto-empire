# BUILD LOG — living record (newest on top, append-only by date)
Every round states: what shipped, what's next, what I assumed, what I decided and why.

## 2026-09-09 — full-kit Poetcore, berry film, CDN throttle call
- Shipped: Poetcore full kit (6 papers, 2 envelopes, 24 prompts, 12 labels) + 10-page PDF; berry-fruit film (blossom→berries, 25s).
- Blocked (external): upload.wikimedia.org 429 throttling our IP ~30 min — lettuce/aerogarden real cuts queued with exact resume (SOURCES.md has URLs + edit recipe). Stopped hammering on purpose.
- Assumption: partial real footage (rain cut live) + procedural engine covers the gap until throttle lifts.
- Decision: no sketchy mirrors/scrapers for licensed footage — wait out the throttle, keep attribution chain clean.
- Next: lettuce/aerogarden cuts on throttle lift → eval goldens for 12 → Poetcore listing goes live the day Etsy shop exists.

## 2026-09-09 — org filings + memory company + live minds + clay + vaults
- Shipped: ORG-PLAN x11, Team 12 (YC R&D, Effect+SQLite memory API, Lab UI), live-thoughts engine, clay 3D, GSAP motion, file vaults, archived 04/07/10.
- Assumption: user views the DEPLOYED app (Vercel), so pushes = releases. Verified pattern holds.
- Decision: archive by flag, not delete — reversibility beats cleanliness while strategy is fluid.
- Decision: LIKE-recall over blocked FTS5 — same API, documented graduation path. Shipping beats purity.
- Next: Poetcore full-kit expansion → lettuce/aerogarden cuts → strawberry film → eval goldens for 12.

## 2026-09-08 — execution era: real video/audio/print, needs list, runner resilience
- Shipped: 3 PIL videos + ffmpeg pipeline, sleep MP3 (SAPI→Piper upgrade), 2 Etsy PDFs, NEEDS.md + app section, Startup shortcut + keepalive wedge-guard.
- Assumption: no keys ever coming — everything must run on OSS/local. Held for all builds since.
- Decision: temp git remotes for push (never stored) — token hygiene over convenience.
- Decision: heartbeat + monitoring passes over fake activity — the bus only ever carries real events.

## 2026-09-03 — foundation: R&D, 10 orgs, no-stop protocol, command deck
- Shipped: cross-platform R&D report, 10 companies, BOOTSTRAP/PROTOCOL/STATE/HANDOFF system, orchestrator, dashboard.
- Assumption: specificity converts (niche-down rule) — validated repeatedly since, never violated.
- Decision: one repo (monorepo) over many — user ordered it; halves ops overhead.
