node_modules
.next
out
dist
.env*
*.log
.DS_Store
# NEVER commit tokens - use env vars only
# render frames are regenerable bulk - never commit
frames/
# local memory DBs (schema lives in repo, data stays on machine)
*.db
*.db-journal
*.db-shm
*.db-wal
# runtime process files (live state, never source)
*.pid
run.log
run.err.log
watchdog.log
